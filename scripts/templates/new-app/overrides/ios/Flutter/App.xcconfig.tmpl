// {{DISPLAY_NAME}} iOS — per-app build settings.
// Regenerated by scripts/new-app.sh {{SLUG}} from apps/{{SLUG}}/app.yaml.

APP_BUNDLE_IDENTIFIER = {{PACKAGE}}
APP_DISPLAY_NAME      = {{DISPLAY_NAME}}
